<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Airbnb Login</title>
</head>

<body class="bg-gray-50 text-gray-800 min-h-screen flex flex-col items-center justify-center p-8">

  <h1 class="text-4xl font-bold text-[#FF5A5F] mb-8">Airbnb Login</h1>

  <form action="/login" method="POST"
        class="bg-white shadow-md rounded-2xl p-6 w-full max-w-md space-y-4 border border-gray-100">
    
    <label class="flex flex-col">
      <span class="text-sm font-medium text-gray-700 mb-1">Username</span>
      <input type="text" name="username" required
             class="rounded-lg border-gray-300 focus:ring-2 focus:ring-[#FF5A5F] focus:outline-none px-3 py-2">
    </label>

    <label class="flex flex-col">
      <span class="text-sm font-medium text-gray-700 mb-1">Password</span>
      <input type="password" name="password" required
             class="rounded-lg border-gray-300 focus:ring-2 focus:ring-[#FF5A5F] focus:outline-none px-3 py-2">
    </label>

    <button type="submit"
            class="w-full px-6 py-2.5 rounded-lg bg-[#FF5A5F] text-white font-semibold hover:bg-red-500 transition">
      Login
    </button>

    <% if (error) { %>
      <p class="text-red-500 text-center mt-2"><%= error %></p>
    <% } %>
  </form>

</body>
</html>
